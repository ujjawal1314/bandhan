# GitHub Configuration

This directory contains repository automation. The `workflows/ci.yml` workflow runs on pushes, prepares Python, installs the planned FastAPI runtime, and validates the Review 1 repository structure.
